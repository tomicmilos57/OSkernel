/*(#include "../lib/mem.h"

void *operator new(uint64 n) { return __mem_alloc(n); }
void *operator new[](uint64 n) { return __mem_alloc(n); }
void operator delete(void *n) noexcept { __mem_free(n); }
void operator delete[](void *n) noexcept { __mem_free(n); }*/